# Server Configuration
PORT=5001
NODE_ENV=development

# Database Configuration
MONGO_URI=mongodb://localhost:27017/owasp-scanner

# Authentication
JWT_SECRET=your_jwt_secret_key_here # Change this to a secure random string in production

# Note: In production, make sure to:
# 1. Use a strong JWT_SECRET
# 2. Use a secured MongoDB instance
# 3. Set NODE_ENV=production